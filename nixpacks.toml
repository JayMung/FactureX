[phases.setup]
nixPkgs = ['nodejs', 'nginx']

[phases.install]
cmds = ['rm -rf node_modules package-lock.json', 'npm install --force']

[phases.build]
cmds = ['npm run build', 'mkdir -p /app/dist', 'cp -r dist/. /app/dist/']

[start]
cmd = 'nginx -c /app/nginx.conf -g "daemon off;"'
